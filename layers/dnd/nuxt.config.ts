// https://nuxt.com/docs/api/configuration/nuxt-config
export default defineNuxtConfig({
  // Extend the main Nuxt 3 configuration for this example
  app: {
    head: {
      title: 'Vue Flow Drag and Drop',
    },
  },
})
